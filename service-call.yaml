apiVersion: v1
kind: Service
metadata:
  name: scipion-master-call
  labels:
    app: scipion-master-call
spec:
  type: NodePort
  selector:
    app: scipion-master-call
  ports:
  - protocol: TCP
    port: 5907
    targetPort: 5907
    nodePort: 30007
